CREATE TABLE EJUD_MPM_BASEDEDADOS (
    ID               NUMBER GENERATED BY DEFAULT AS IDENTITY,
    CODDOC           NUMBER,
    DTHRMOV          DATE,
    CODFASE          NUMBER,
    CODCOMPL1        NUMBER,
    CODCOMPL2        NUMBER,
    CODCOMPL3        NUMBER,
    CODLOCAL         NUMBER,
    TIPOMOV          VARCHAR2(20),
    DTETL            DATE
);

SELECT * FROM EJUD_MPM_BASEDEDADOS;

CREATE UNIQUE INDEX EJUD_MPM_BDD_UNIQUE_IDX
ON EJUD_MPM_BASEDEDADOS (CODDOC, DTHRMOV, TIPOMOV);   